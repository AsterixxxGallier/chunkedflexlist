import {HollowChunkedFlexList} from "../hollowChunkedFlexList"

describe("AbstractChunkedFlexList", () => {
	test("AbstractChunkedFlexList.makeSpace", () => {
		const list = new HollowChunkedFlexList()
	})
})